# 股票监控系统 - 香港服务器部署教程

## 📋 部署概览

- **服务器**：阿里云香港轻量应用服务器
- **配置**：2核2G（约¥24/月）或 2核4G（约¥34/月）
- **系统**：Ubuntu 22.04
- **优势**：无需备案，直接用IP访问
- **部署时间**：约30分钟

---

## 第一步：购买香港服务器

### 1.1 注册阿里云账号
1. 访问：https://www.aliyun.com
2. 点击右上角"免费注册"
3. 使用手机号注册并完成实名认证

### 1.2 购买轻量应用服务器
1. 访问：https://www.aliyun.com/product/swas
2. 点击"立即购买"
3. 选择配置：
   - **地域**：中国香港
   - **镜像**：系统镜像 → Ubuntu 22.04
   - **套餐**：2核2G 30GB（¥24/月）或 2核4G 60GB（¥34/月，推荐）
   - **购买时长**：按需选择（建议先买1个月测试）
4. 勾选服务协议，点击"立即购买"
5. 完成支付

### 1.3 获取服务器信息
1. 进入控制台：https://swas.console.aliyun.com/
2. 找到你的服务器实例
3. 记录以下信息：
   - **公网IP**：例如 `43.123.45.67`
   - **用户名**：`root`
   - **密码**：点击"重置密码"设置一个新密码

---

## 第二步：连接服务器

### 2.1 使用阿里云网页终端（推荐新手）
1. 在服务器控制台，点击"远程连接"
2. 选择"Workbench远程连接"
3. 输入用户名 `root` 和密码
4. 进入命令行界面

### 2.2 使用本地SSH工具（可选）
**Windows用户**：
1. 按 `Win + R`，输入 `cmd`
2. 执行：
```bash
ssh root@你的服务器IP
```
3. 输入密码登录

---

## 第三步：安装Docker环境

### 3.1 更新系统
```bash
apt update && apt upgrade -y
```

### 3.2 安装Docker
```bash
# 安装Docker
curl -fsSL https://get.docker.com | sh

# 启动Docker
systemctl start docker
systemctl enable docker

# 验证安装
docker --version
```

### 3.3 安装Docker Compose
```bash
# 下载Docker Compose
curl -L "https://github.com/docker/compose/releases/download/v2.24.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose

# 添加执行权限
chmod +x /usr/local/bin/docker-compose

# 验证安装
docker-compose --version
```

---

## 第四步：上传项目文件

### 4.1 在服务器创建目录
```bash
mkdir -p /root/stock
cd /root/stock
```

### 4.2 上传文件（方法一：使用SCP）
**在你的本地电脑**打开命令提示符：
```bash
# 进入项目目录
cd C:\code\Stock

# 上传整个项目（替换为你的服务器IP）
scp -r * root@你的服务器IP:/root/stock/
```

输入服务器密码后开始上传。

### 4.3 上传文件（方法二：使用阿里云控制台）
1. 在服务器控制台，点击"文件传输"
2. 选择本地文件，上传到 `/root/stock/` 目录
3. 上传以下文件：
   - `docker-compose.yml`
   - `Dockerfile`
   - `init.sql`
   - `pom.xml`
   - `src/` 目录
   - `stock-frontend/` 目录

### 4.4 验证文件
```bash
cd /root/stock
ls -la
```

应该看到所有项目文件。

---

## 第五步：配置前端API地址

### 5.1 修改前端配置
```bash
# 编辑前端配置文件
nano /root/stock/stock-frontend/.env.production
```

### 5.2 修改内容
将内容改为（替换为你的服务器IP）：
```
VITE_API_BASE_URL=http://你的服务器IP:8080
```

按 `Ctrl + O` 保存，按 `Ctrl + X` 退出。

---

## 第六步：配置防火墙

### 6.1 在阿里云控制台开放端口
1. 进入服务器控制台
2. 点击"防火墙"
3. 添加规则：
   - **端口**：80
   - **协议**：TCP
   - **策略**：允许
   - **备注**：前端访问
4. 再添加一条：
   - **端口**：8080
   - **协议**：TCP
   - **策略**：允许
   - **备注**：后端API

---

## 第七步：启动系统

### 7.1 构建并启动容器
```bash
cd /root/stock
docker-compose up -d --build
```

**说明**：
- 首次构建需要10-15分钟
- 看到 "done" 表示启动成功

### 7.2 查看启动状态
```bash
docker-compose ps
```

应该看到3个容器都在运行：
- `stock-mysql`
- `stock-backend`
- `stock-frontend`

### 7.3 查看日志（如有问题）
```bash
# 查看所有日志
docker-compose logs

# 查看后端日志
docker-compose logs backend

# 实时查看日志
docker-compose logs -f
```

---

## 第八步：访问系统

### 8.1 浏览器访问
打开浏览器，访问：
```
http://你的服务器IP
```

例如：`http://43.123.45.67`

### 8.2 默认账号
- **用户名**：`admin`
- **密码**：`admin123`

### 8.3 分享给用户
将访问地址分享给用户：
```
访问地址：http://你的服务器IP
用户名：admin
密码：admin123
```

---

## 🔄 日常管理

### 启动服务
```bash
cd /root/stock
docker-compose up -d
```

### 停止服务
```bash
cd /root/stock
docker-compose down
```

### 重启服务
```bash
cd /root/stock
docker-compose restart
```

### 查看日志
```bash
cd /root/stock
docker-compose logs -f
```

### 更新代码
```bash
# 1. 上传新文件到服务器
# 2. 重新构建并启动
cd /root/stock
docker-compose down
docker-compose up -d --build
```

---

## 🔒 安全加固（可选但推荐）

### 1. 修改默认管理员密码
登录系统后，立即修改 `admin` 账号密码。

### 2. 配置HTTPS（可选）
如果有域名，可以配置免费SSL证书：
```bash
# 安装certbot
apt install certbot -y

# 申请证书（替换为你的域名）
certbot certonly --standalone -d yourdomain.com
```

### 3. 限制SSH登录
```bash
# 编辑SSH配置
nano /etc/ssh/sshd_config

# 修改以下内容：
PermitRootLogin no  # 禁止root直接登录
PasswordAuthentication no  # 禁用密码登录，只用密钥

# 重启SSH服务
systemctl restart sshd
```

---

## ⚠️ 常见问题

### 问题1：无法访问网站
**检查清单**：
1. 防火墙是否开放80和8080端口？
2. Docker容器是否正常运行？`docker-compose ps`
3. 前端配置的API地址是否正确？

### 问题2：后端API报错
**解决**：
```bash
# 查看后端日志
docker-compose logs backend

# 重启后端
docker-compose restart backend
```

### 问题3：数据库连接失败
**解决**：
```bash
# 查看数据库日志
docker-compose logs mysql

# 重启数据库
docker-compose restart mysql
```

### 问题4：前端页面空白
**解决**：
1. 检查浏览器控制台错误（F12）
2. 确认 `.env.production` 中的API地址正确
3. 重新构建前端：
```bash
docker-compose restart frontend
```

---

## 💰 费用说明

### 月度费用
- **2核2G套餐**：¥24/月
- **2核4G套餐**：¥34/月（推荐）

### 流量说明
- 包含1TB/月流量
- 100人使用完全够用
- 超出流量按量计费（约¥0.8/GB）

### 续费优惠
- 首次购买可能有新用户优惠
- 长期购买（半年/一年）有折扣

---

## 📊 性能预估

### 2核2G配置
- **并发用户**：20-30人
- **适用场景**：小团队内部使用

### 2核4G配置（推荐）
- **并发用户**：50-80人
- **适用场景**：中小型团队

### 如需支持100+用户
建议升级到：
- **4核8G**：¥68/月
- **并发用户**：100-150人

---

## 🎯 优势总结

✅ **无需备案**：直接用IP访问，省去15-20天备案时间  
✅ **24小时运行**：不占用个人电脑资源  
✅ **稳定可靠**：阿里云机房，99.9%可用性  
✅ **访问速度快**：香港节点，国内访问延迟低  
✅ **易于管理**：Docker一键部署，方便更新  
✅ **成本可控**：按月付费，随时可停  

---

## 📞 技术支持

遇到问题请提供：
1. 错误截图
2. 日志内容：`docker-compose logs`
3. 服务器IP和配置信息

---

## 🔗 相关链接

- 阿里云控制台：https://swas.console.aliyun.com/
- Docker官方文档：https://docs.docker.com/
- Ubuntu官方文档：https://ubuntu.com/server/docs
