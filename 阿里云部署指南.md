# 股票监控系统 - 阿里云服务器部署指南

## 📋 服务器信息
- **公网IP**: 47.236.199.133
- **配置**: 2核1GB内存，30GB存储
- **系统**: Ubuntu 22.04
- **地域**: 中国香港

---

## 🚀 快速部署（推荐）

### 方法一：使用自动部署脚本

#### 1. 上传项目文件到服务器

在**本地电脑**打开PowerShell，执行：

```powershell
# 进入项目目录
cd C:\code\Stock

# 使用SCP上传整个项目（需要输入密码：Hr%rK+3!x3B_4Bi）
scp -r * root@47.236.199.133:/root/stock/
```

**或者使用阿里云控制台上传**：
1. 登录阿里云控制台
2. 进入服务器管理页面
3. 点击"文件传输"
4. 上传以下文件到 `/root/stock/` 目录：
   - `Dockerfile.optimized`
   - `docker-compose.prod.yml`
   - `deploy.sh`
   - `pom.xml`
   - `init.sql`
   - `src/` 目录（完整）
   - `stock-frontend/` 目录（完整）

#### 2. 连接服务器

使用阿里云网页终端：
1. 在控制台点击"远程连接"
2. 选择"Workbench远程连接"
3. 输入用户名 `root` 和密码 `Hr%rK+3!x3B_4Bi`

#### 3. 执行部署脚本

```bash
cd /root/stock
cd /root/stock
./deploy.sh
```

**等待10-15分钟**，脚本会自动完成：
- ✅ 检查Docker环境
- ✅ 构建镜像
- ✅ 启动容器
- ✅ 健康检查

#### 4. 访问系统

浏览器打开：`http://47.236.199.133`

默认账号：
- 用户名：`admin`
- 密码：`admin123`

---

## 🔧 手动部署（详细步骤）

如果自动脚本失败，可以手动执行以下步骤：

### 步骤1：安装Docker

```bash
# 更新系统
apt update && apt upgrade -y

# 安装Docker
curl -fsSL https://get.docker.com | sh

# 启动Docker
systemctl start docker
systemctl enable docker

# 验证安装
docker --version
```

### 步骤2：安装Docker Compose

```bash
# 下载Docker Compose
curl -L "https://github.com/docker/compose/releases/download/v2.24.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose

# 添加执行权限
chmod +x /usr/local/bin/docker-compose

# 验证安装
docker-compose --version
```

### 步骤3：上传项目文件

确保 `/root/stock/` 目录包含：
```
/root/stock/
├── Dockerfile.optimized
├── docker-compose.prod.yml
├── pom.xml
├── init.sql
├── src/
│   └── main/
│       ├── java/
│       └── resources/
└── stock-frontend/
    ├── Dockerfile
    ├── nginx.conf
    ├── package.json
    └── src/
```

### 步骤4：构建并启动

```bash
cd /root/stock

# 构建镜像（需要10-15分钟）
docker-compose -f docker-compose.prod.yml build

# 启动容器
docker-compose -f docker-compose.prod.yml up -d

# 查看启动状态
docker-compose -f docker-compose.prod.yml ps
```

### 步骤5：查看日志

```bash
# 查看所有日志
docker-compose -f docker-compose.prod.yml logs -f

# 只看后端日志
docker-compose -f docker-compose.prod.yml logs -f backend

# 只看数据库日志
docker-compose -f docker-compose.prod.yml logs -f mysql
```

---

## 📊 内存优化说明

针对1GB内存服务器，已做以下优化：

### MySQL优化
- 最大连接数：50
- InnoDB缓冲池：128MB
- 内存限制：256MB

### Spring Boot优化
- 堆内存：256MB-384MB
- 元空间：128MB-256MB
- 使用G1垃圾回收器

### Nginx优化
- 内存限制：128MB

**总内存分配**：
- MySQL: 256MB
- Backend: 512MB
- Frontend: 128MB
- 系统预留: ~150MB

---

## 🔄 日常管理命令

### 启动服务
```bash
cd /root/stock
docker-compose -f docker-compose.prod.yml up -d
```

### 停止服务
```bash
cd /root/stock
docker-compose -f docker-compose.prod.yml down
```

### 重启服务
```bash
cd /root/stock
docker-compose -f docker-compose.prod.yml restart
```

### 重启单个服务
```bash
# 重启后端
docker-compose -f docker-compose.prod.yml restart backend

# 重启前端
docker-compose -f docker-compose.prod.yml restart frontend

# 重启数据库
docker-compose -f docker-compose.prod.yml restart mysql
```

### 查看容器状态
```bash
docker-compose -f docker-compose.prod.yml ps
```

### 查看资源占用
```bash
docker stats
```

### 进入容器
```bash
# 进入后端容器
docker exec -it stock-backend sh

# 进入数据库容器
docker exec -it stock-mysql bash

# 进入前端容器
docker exec -it stock-frontend sh
```

---

## 🔍 故障排查

### 问题1：容器启动失败

**检查日志**：
```bash
docker-compose -f docker-compose.prod.yml logs
```

**常见原因**：
- 端口被占用（80或8080）
- 内存不足
- 镜像构建失败

**解决方法**：
```bash
# 停止所有容器
docker-compose -f docker-compose.prod.yml down

# 清理系统
docker system prune -a -f

# 重新构建
docker-compose -f docker-compose.prod.yml build --no-cache
docker-compose -f docker-compose.prod.yml up -d
```

### 问题2：后端无法连接数据库

**检查数据库状态**：
```bash
docker-compose -f docker-compose.prod.yml logs mysql
```

**手动测试连接**：
```bash
docker exec -it stock-mysql mysql -ustock_user -pstock123 stock_monitor_db
```

### 问题3：前端无法访问

**检查Nginx配置**：
```bash
docker exec -it stock-frontend cat /etc/nginx/conf.d/default.conf
```

**检查前端日志**：
```bash
docker-compose -f docker-compose.prod.yml logs frontend
```

### 问题4：内存不足（OOM）

**查看内存使用**：
```bash
free -h
docker stats
```

**临时解决**：
```bash
# 重启服务释放内存
docker-compose -f docker-compose.prod.yml restart
```

**永久解决**：
- 升级服务器配置到2GB内存
- 或减少并发用户数

---

## 🔒 安全建议

### 1. 修改默认密码
登录系统后，立即修改admin账号密码。

### 2. 配置防火墙
在阿里云控制台配置安全组规则：
- 只开放必要端口：80、8080、22
- 限制SSH访问IP（可选）

### 3. 定期备份数据库
```bash
# 备份数据库
docker exec stock-mysql mysqldump -uroot -pstock123 stock_monitor_db > backup_$(date +%Y%m%d).sql

# 恢复数据库
docker exec -i stock-mysql mysql -uroot -pstock123 stock_monitor_db < backup_20260526.sql
```

### 4. 更新系统
```bash
apt update && apt upgrade -y
```

---

## 📈 性能监控

### 查看系统资源
```bash
# CPU和内存
htop

# 磁盘使用
df -h

# Docker容器资源
docker stats
```

### 查看应用日志
```bash
# 实时查看后端日志
docker-compose -f docker-compose.prod.yml logs -f backend

# 查看最近100行
docker-compose -f docker-compose.prod.yml logs --tail=100 backend
```

---

## 🆘 紧急情况处理

### 服务器无响应
```bash
# 重启所有容器
docker-compose -f docker-compose.prod.yml restart

# 如果还是不行，重启Docker
systemctl restart docker
```

### 磁盘空间不足
```bash
# 清理Docker缓存
docker system prune -a -f

# 清理日志
docker-compose -f docker-compose.prod.yml logs --tail=0 -f > /dev/null &
```

### 数据库损坏
```bash
# 停止服务
docker-compose -f docker-compose.prod.yml down

# 删除数据卷（会丢失数据！）
docker volume rm stock_mysql-data

# 重新启动（会重新初始化数据库）
docker-compose -f docker-compose.prod.yml up -d
```

---

## 📞 技术支持

遇到问题请提供：
1. 错误截图
2. 日志内容：`docker-compose -f docker-compose.prod.yml logs`
3. 容器状态：`docker-compose -f docker-compose.prod.yml ps`
4. 系统资源：`free -h` 和 `df -h`

---

## 📝 更新日志

### v1.0 (2026-05-26)
- ✅ 初始部署
- ✅ 内存优化（适配1GB服务器）
- ✅ 自动部署脚本
- ✅ 健康检查配置
