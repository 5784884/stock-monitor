# 🚀 股票监控系统 - 快速部署清单

## ✅ 已准备的文件

我已经为你创建了以下优化配置文件：

1. **Dockerfile.optimized** - 优化的后端Docker镜像（适配1GB内存）
2. **docker-compose.prod.yml** - 生产环境Docker编排配置
3. **deploy.sh** - 自动部署脚本
4. **upload.ps1** - Windows上传脚本
5. **阿里云部署指南.md** - 详细部署文档

---

## 📋 部署步骤（3步完成）

### 第一步：上传文件到服务器

**推荐方式：使用阿里云控制台**

1. 登录阿里云控制台：https://swas.console.aliyun.com/
2. 找到你的服务器 `Ubuntu-igxg`
3. 点击"文件传输"
4. 上传以下文件到 `/root/stock/` 目录：

```
必需文件：
✓ Dockerfile.optimized
✓ docker-compose.prod.yml
✓ deploy.sh
✓ pom.xml
✓ init.sql
✓ src/ (整个目录)
✓ stock-frontend/ (整个目录)
```

**或者运行上传脚本**：
- 双击 `upload.ps1` 查看上传命令
- 按照提示手动上传

---

### 第二步：连接服务器

1. 在阿里云控制台点击"远程连接"
2. 选择"Workbench远程连接"
3. 输入：
   - 用户名：`root`
   - 密码：`Hr%rK+3!x3B_4Bi`

---

### 第三步：执行部署

在服务器终端执行：

```bash
cd /root/stock
chmod +x deploy.sh
./deploy.sh
```

**等待10-15分钟**，脚本会自动完成所有部署。

---

## 🎉 部署完成后

### 访问系统

浏览器打开：**http://47.236.199.133**

### 默认账号

- 用户名：`admin`
- 密码：`admin123`

---

## 🔧 常用管理命令

```bash
# 查看运行状态
docker-compose -f docker-compose.prod.yml ps

# 查看日志
docker-compose -f docker-compose.prod.yml logs -f

# 重启服务
docker-compose -f docker-compose.prod.yml restart

# 停止服务
docker-compose -f docker-compose.prod.yml down

# 启动服务
docker-compose -f docker-compose.prod.yml up -d
```

---

## ⚠️ 重要提示

### 内存优化
已针对1GB内存优化：
- MySQL: 256MB
- 后端: 512MB
- 前端: 128MB

### 性能限制
- 建议并发用户：20-30人
- 如需支持更多用户，建议升级到2GB内存

### 防火墙配置
确保阿里云安全组已开放：
- 端口 80 (HTTP)
- 端口 8080 (后端API，可选)
- 端口 22 (SSH)

---

## 🆘 遇到问题？

### 查看日志
```bash
cd /root/stock
docker-compose -f docker-compose.prod.yml logs
```

### 重新部署
```bash
cd /root/stock
docker-compose -f docker-compose.prod.yml down
./deploy.sh
```

### 检查资源
```bash
# 查看内存
free -h

# 查看磁盘
df -h

# 查看容器资源
docker stats
```

---

## 📞 技术支持

详细文档请查看：`阿里云部署指南.md`

---

## ✨ 优化亮点

✅ **内存优化** - 适配1GB小内存服务器  
✅ **自动部署** - 一键脚本，无需手动配置  
✅ **健康检查** - 自动监控服务状态  
✅ **数据持久化** - MySQL数据不会丢失  
✅ **自动重启** - 容器异常自动恢复  
✅ **日志管理** - 方便排查问题  

---

## 🎯 下一步

部署成功后：
1. ✅ 修改admin密码
2. ✅ 配置定期备份
3. ✅ 监控系统资源
4. ✅ 测试所有功能

---

**祝部署顺利！** 🚀
