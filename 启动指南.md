# 股票监控系统启动指南（免费临时地址版）

## 🚀 快速启动（推荐）

### 方法一：使用一键启动脚本
直接双击 `一键启动.bat`，等待约 1 分钟，会自动显示访问地址。

---

## 📝 手动启动步骤

### 第一步：启动后端服务
```bash
cd C:\code\Stock
mvn spring-boot:run
```
**等待 30 秒**，看到 `Started StockApplication` 表示启动成功。

---

### 第二步：启动后端内网穿透
**新开一个终端窗口**，执行：
```bash
cd C:\code\Stock
./cloudflared.exe tunnel --url http://localhost:8080 > cloudflared-backend.log 2>&1 &
```

**等待 10 秒**，然后获取后端地址：
```bash
grep -a "trycloudflare.com" cloudflared-backend.log | grep -oE "https://[a-z0-9-]+\.trycloudflare\.com" | head -1
```

**重要：复制这个地址**，例如：`https://xxx-xxx-xxx-xxx.trycloudflare.com`

---

### 第三步：更新前端配置
编辑文件：`C:\code\Stock\stock-frontend\.env.development`

将内容改为（替换为你刚才复制的后端地址）：
```
VITE_API_BASE_URL=https://xxx-xxx-xxx-xxx.trycloudflare.com
```

**保存文件！**

---

### 第四步：启动前端服务
```bash
cd C:\code\Stock\stock-frontend
npm run dev -- --host 0.0.0.0 --port 5176
```

**等待 10 秒**，看到 `Local: http://localhost:5176/` 表示启动成功。

---

### 第五步：启动前端内网穿透
**新开一个终端窗口**，执行：
```bash
cd C:\code\Stock
./cloudflared.exe tunnel --url http://localhost:5176 > cloudflared-frontend.log 2>&1 &
```

**等待 10 秒**，然后获取前端地址：
```bash
grep -a "trycloudflare.com" cloudflared-frontend.log | grep -oE "https://[a-z0-9-]+\.trycloudflare\.com" | head -1
```

**这就是你的访问地址！** 例如：`https://yyy-yyy-yyy-yyy.trycloudflare.com`

---

### 第六步：访问系统
在浏览器打开前端地址，登录：
- 用户名：`admin`
- 密码：`admin123`

**如果登录失败**，按 `Ctrl+Shift+R` 强制刷新浏览器缓存。

---

## 🔄 重启服务（地址会变）

每次重启服务，地址都会改变，需要重新执行以下步骤：

### 1. 停止所有服务
双击 `停止服务.bat` 或手动执行：
```bash
taskkill /F /IM java.exe
taskkill /F /IM node.exe
taskkill /F /IM cloudflared.exe
```

### 2. 清理日志文件
```bash
cd C:\code\Stock
del cloudflared-backend.log
del cloudflared-frontend.log
```

### 3. 重新执行上面的启动步骤
**关键：** 必须先获取新的后端地址，更新前端配置，再启动前端！

---

## ⚠️ 常见问题

### 1. 登录失败，提示网络错误
**原因：** 前端配置的后端地址不正确或后端隧道未启动

**解决：**
1. 检查 `stock-frontend\.env.development` 中的地址是否正确
2. 测试后端隧道是否正常：
   ```bash
   curl https://你的后端地址/api/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"admin123"}'
   ```
   应该返回 token

### 2. 前端页面空白或报错
**原因：** 浏览器缓存了旧的配置

**解决：** 按 `Ctrl+Shift+R` 强制刷新，或清除浏览器缓存

### 3. 隧道连接失败
**原因：** 网络问题或 cloudflared 进程异常

**解决：**
1. 检查网络连接
2. 重启 cloudflared 进程
3. 查看日志文件排查错误

### 4. 后端启动失败
**原因：** MySQL 未运行或端口被占用

**解决：**
1. 检查 MySQL 是否运行：`netstat -ano | findstr "3306"`
2. 检查 8080 端口是否被占用：`netstat -ano | findstr "8080"`
3. 查看 `backend.log` 日志

---

## 📋 启动检查清单

启动前确认：
- [ ] MySQL 数据库已启动（3306 端口）
- [ ] 8080 和 5176 端口未被占用
- [ ] 网络连接正常

启动后确认：
- [ ] 后端服务启动成功（看到 `Started StockApplication`）
- [ ] 后端隧道已建立（获取到 https 地址）
- [ ] 前端配置已更新（`.env.development` 文件）
- [ ] 前端服务启动成功（看到 `Local: http://localhost:5176/`）
- [ ] 前端隧道已建立（获取到 https 地址）
- [ ] 浏览器可以访问前端地址
- [ ] 可以成功登录系统

---

## 💡 提示

1. **地址会变化**：每次重启服务，trycloudflare.com 的地址都会改变
2. **配置要更新**：每次重启必须更新前端配置文件中的后端地址
3. **顺序很重要**：必须先启动后端隧道 → 更新前端配置 → 启动前端
4. **保存好地址**：启动后立即保存前端访问地址，方便使用
5. **建议购买域名**：如果需要固定地址，建议购买域名（约 10-50 元/年）

---

## 🎯 一键启动脚本说明

`一键启动.bat` 会自动完成以下操作：
1. 启动后端服务
2. 启动后端隧道并获取地址
3. 自动更新前端配置文件
4. 启动前端服务
5. 启动前端隧道并显示访问地址

**总耗时约 1 分钟**，推荐使用！

---

## 🛑 停止服务

双击 `停止服务.bat` 或手动执行：
```bash
taskkill /F /IM java.exe
taskkill /F /IM node.exe
taskkill /F /IM cloudflared.exe
```

---

## 📞 需要帮助？

如果遇到问题：
1. 查看日志文件：`backend.log`、`frontend.log`、`cloudflared-*.log`
2. 检查服务状态：`ps aux | grep -E "(java|node|cloudflared)"`
3. 测试端口连接：`curl http://localhost:8080` 和 `curl http://localhost:5176`
