
# 固定公网地址配置指南

## 方案一：Cloudflare Tunnel（推荐，免费）

### 前提条件
- 拥有一个域名（可在阿里云/腾讯云购买，约 10-50 元/年）
- 域名已添加到 Cloudflare 并修改 DNS 服务器

---

## 详细配置步骤

### 1. 登录 Cloudflare
```bash
cd C:\code\Stock
./cloudflared.exe tunnel login
```
浏览器会打开，选择你的域名授权。

### 2. 创建命名隧道
```bash
./cloudflared.exe tunnel create stock-system
```

**重要**：记录输出的隧道 ID，例如：`abc123-def456-ghi789`

### 3. 配置 DNS 路由
```bash
# 前端域名
./cloudflared.exe tunnel route dns stock-system stock.你的域名.com

# 后端域名
./cloudflared.exe tunnel route dns stock-system api.你的域名.com
```

### 4. 修改配置文件
编辑 `C:\code\Stock\cloudflared-config.yml`：

```yaml
tunnel: abc123-def456-ghi789  # 替换为你的隧道ID
credentials-file: C:\Users\宋\.cloudflared\abc123-def456-ghi789.json  # 替换为你的隧道ID

ingress:
  # 前端服务
  - hostname: stock.你的域名.com  # 替换为你的域名
    service: http://localhost:5176

  # 后端API服务
  - hostname: api.你的域名.com  # 替换为你的域名
    service: http://localhost:8080

  # 默认规则（必须）
  - service: http_status:404
```

### 5. 修改前端配置
编辑 `C:\code\Stock\stock-frontend\.env.development`：

```bash
VITE_API_BASE_URL=https://api.你的域名.com  # 替换为你的域名
```

### 6. 启动服务

#### 方式1：使用脚本启动
双击 `start-tunnel.bat`

#### 方式2：手动启动
```bash
cd C:\code\Stock
./cloudflared.exe tunnel --config cloudflared-config.yml run stock-system
```

### 7. 访问系统
- 前端：`https://stock.你的域名.com`
- 后端：`https://api.你的域名.com`
- 登录：admin / admin123

---

## 方案二：使用 frp（适合有公网服务器的情况）

### 前提条件
- 拥有一台有公网 IP 的服务器

### 服务器端配置（frps）
1. 下载 frp：https://github.com/fatedier/frp/releases
2. 配置 `frps.ini`：
```ini
[common]
bind_port = 7000
vhost_http_port = 80
vhost_https_port = 443
```

3. 启动服务端：
```bash
./frps -c frps.ini
```

### 客户端配置（frpc）
1. 下载 frp 客户端
2. 配置 `frpc.ini`：
```ini
[common]
server_addr = 你的服务器IP
server_port = 7000

[stock-frontend]
type = http
local_port = 5176
custom_domains = stock.你的域名.com

[stock-backend]
type = http
local_port = 8080
custom_domains = api.你的域名.com
```

3. 启动客户端：
```bash
./frpc -c frpc.ini
```

---

## 方案三：使用花生壳/Ngrok（付费，简单）

### 花生壳
1. 注册账号：https://hsk.oray.com/
2. 下载客户端
3. 购买域名映射服务（约 6 元/月）
4. 配置端口映射：
   - 5176 → stock.你的域名.com
   - 8080 → api.你的域名.com

### Ngrok
1. 注册账号：https://ngrok.com/
2. 下载客户端
3. 配置 `ngrok.yml`：
```yaml
authtoken: 你的token
tunnels:
  frontend:
    proto: http
    addr: 5176
    hostname: stock.你的域名.com
  backend:
    proto: http
    addr: 8080
    hostname: api.你的域名.com
```

4. 启动：
```bash
ngrok start --all
```

---

## 推荐方案对比

| 方案 | 费用 | 稳定性 | 速度 | 难度 |
|------|------|--------|------|------|
| Cloudflare Tunnel | 免费 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |
| frp | 服务器费用 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 花生壳 | 6元/月 | ⭐⭐⭐ | ⭐⭐⭐ | ⭐ |
| Ngrok | 8美元/月 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |

**推荐使用 Cloudflare Tunnel**：免费、稳定、配置简单。

---

## 常见问题

### 1. 域名在哪里购买？
- 阿里云：https://wanwang.aliyun.com/
- 腾讯云：https://dnspod.cloud.tencent.com/
- Cloudflare：https://www.cloudflare.com/products/registrar/

推荐购买 `.com` 或 `.cn` 域名，价格约 10-50 元/年。

### 2. 如何将域名添加到 Cloudflare？
1. 登录 Cloudflare：https://dash.cloudflare.com/
2. 点击 "添加站点"
3. 输入你的域名
4. 选择免费计划
5. 按照提示修改域名的 DNS 服务器

### 3. 隧道启动失败怎么办？
- 检查配置文件路径是否正确
- 确认隧道 ID 和凭证文件匹配
- 查看 `cloudflared.log` 日志

### 4. 如何设置开机自启动？
创建 Windows 计划任务：
1. 打开"任务计划程序"
2. 创建基本任务
3. 触发器：系统启动时
4. 操作：启动程序 `C:\code\Stock\start-tunnel.bat`

---

## 完整启动流程（使用固定域名）

### 1. 启动后端
```bash
cd C:\code\Stock
mvn spring-boot:run
```

### 2. 启动前端
```bash
cd stock-frontend
npm run dev -- --host 0.0.0.0 --port 5176
```

### 3. 启动隧道
```bash
cd C:\code\Stock
./cloudflared.exe tunnel --config cloudflared-config.yml run stock-system
```

### 4. 访问系统
浏览器打开：`https://stock.你的域名.com`

---

## 生产环境建议

1. **使用 Docker 部署**：更稳定、易管理
2. **配置 HTTPS 证书**：Cloudflare 自动提供
3. **启用 CDN 加速**：Cloudflare 自带
4. **配置防火墙规则**：保护后端安全
5. **定期备份数据库**：避免数据丢失

详见：`客户部署手册-Docker版.md`
